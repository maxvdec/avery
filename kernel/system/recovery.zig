const ata = @import("ata");
const out = @import("output");
const dir_structure = @import("dir_structure");

pub fn systemIntegrityChecks() bool {
    // These are the following system integrity checks that the program
}
